// 定义节点分组类型
export enum NodeGroupType {
  Audience,
}

// 定义节点类型
export enum NodeType {
  ELEMENT,
  CONFIG,
}

// 定义边的类型
export enum EdgeType {
  Start,
  Success,
  Fail,
  // MatchSuccess,
  // MatchFailure,
}
